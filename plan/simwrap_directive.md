# Directive: simulation wrap-up track (owner-approved 2026-08-18)

Two items, run as one phase with separate frozen manifests. Adjudication §C-2 plus the independent-closure graduation follow-up. §C-3 (degeneracy-breaking wrench task), the connector/tip-mass axis, and everything real-robot stay OUT of scope (ideation ongoing; do not start).

## Item 1: k-interaction adaptation curve (§C-2, the promised headline metric)

Question: how does performance improve with the NUMBER OF INTERACTIONS k, starting from a cold start? This is the interaction-count axis (k = 0, 1, 2, 4, 8, 16, 32 or the ralplan's variant), distinct from the already-reported within-interaction frame-truncation axis; never conflate the two in naming (A-22).

Design constraints (ralplan refines and freezes the rest from prior committed artifacts):
- Cold start per the proposal: k=0 = the property-blind prior; the curve is read against the privileged-teacher ceiling.
- Acquisition rule FROZEN in the manifest before data (which grasp/template the (k+1)-th interaction uses, as a function of the current belief or as a fixed schedule; either is acceptable if frozen; label which honesty class the choice supports: a fixed schedule tests information-sufficiency vs k, an on-policy rule tests the deployment protocol).
- At each k report: held-out map RMSE, boundary/band error, and selection regret, with the multi-seed variability discipline (A-16), unique-(B,w)-group aggregation with ratio pairs as invariance controls (A-15).
- Primary task: lift-and-clear (where the task-only student is pre-registered PASS). Secondary: distal with the spanning cohort. Baselines: teacher (ceiling), blind (flat line), leak-free sysID per k.
- New seed banks disjoint from all prior banks.

## Item 2: r_N-corrected no-refit gate (graduates the independent-closure NULL)

Small, cheap, mostly recompute. Pre-register: predict the gravity self-weight sag from B_eff_force / r_N (the analytical lumped-mass correction, finding 5) with NO refit, against the frozen bound the ralplan derives from prior committed numbers; include the held-out prospective cohort (new lengths/masses) so it is a prediction, not a re-description. The mandatory claim boundary from the independent closure carries over verbatim (same-simulator, different-load-law, baseline-subtracted; now r_N-corrected). A PASS graduates the closure; a miss is reported as-is. The ~1.3pp unexplained residual stays an honest open item either way; do not tune it away.

## Process

Unchanged: ralplan first (thresholds only from prior committed artifacts), single-file manifest freezes before data, independent red-team recompute, figures mandated (adaptation curve with baselines and ceiling; r_N-corrected prediction overlay), STATUS entries as dated sections, descriptive commits, STOP at closure for the owner ruling.
